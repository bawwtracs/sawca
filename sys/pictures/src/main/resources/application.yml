server:
  port: 18080
  tomcat:
    max-threads: 200
    uri-encoding: UTF-8
logging:
  file:
    max-history: 30
  level:
    root: warn
spring:
#  http:
#    multipart:
#      maxFileSize: 1000Mb
#      maxRequestSize: 1000Mb
  mvc:
    static-path-pattern: /static/**
  resources:
    static-locations: classpath:/static/
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pictures?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=true
    username: root
    password: mc2019go
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
    show-sql: true
wozhitu:
  api:
    host: http://www.tudatech.com:8086
    key-fast: guangzhouMrHuangGongAnProj20191126H1_simph2type2
    key-accurate: guangzhouMrHuangGongAnProj20191126H1_recog
    search: /vsearchtech/api/v1.0/apisim_search
    add: /vsearchtech/api/v1.0/apisim_additem
